# Full tree push status

Public concept core is on `main` (README, MIT, oracle/vault contracts, deploy tag, adapters + testnet verification docs, template.json).

**Remaining Scaffold-HBAR starter files** (full `packages/nextjs`, hardhat scripts, package-lock, binary assets) are ready on the builder box as git bundle / tarball. Owner (or Bot with `gh` auth) can push the remainder.

Until the full tree lands, `npm create scaffold-hbar@latest -- --template inunanba/oracle-gated-vault` is **not** complete. Local path on builder box is the source of truth for CI-green builds.
